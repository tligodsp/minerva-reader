import React from 'react';
import Slider from "react-slick";

const TestPage = (props) => {
  const settings = {
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 3,
    slidesToScroll: 3
  };

  return (
    <div>
      Alo
    </div>
  );
}

export default TestPage;
